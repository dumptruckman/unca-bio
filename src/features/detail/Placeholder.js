import React from 'react';
import Typography from '@material-ui/core/Typography';

const Placeholder = ({ classes, data: { locality } }) => (
  <div>
    <Typography>Placeholder</Typography>
  </div>
);

export default Placeholder;
